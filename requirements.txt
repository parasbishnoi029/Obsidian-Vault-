# Core - all pure-Python or have prebuilt wheels for current Python versions.
# No compiled/Rust dependencies here on purpose (see rag/vectorstore.py and
# rag/embeddings.py for why ChromaDB was removed).
streamlit>=1.38.0
numpy
google-genai>=1.0.0

# Optional: only needed if you pick "Local (sentence-transformers)" in the
# sidebar embedding backend. Left commented out by default so cloud deploys
# stay light and don't risk an out-of-memory crash from PyTorch on the free
# 1GB RAM tier, and don't risk PyTorch's own wheel-availability issues on
# very new Python versions. Uncomment only for local/offline use.
# sentence-transformers>=3.0.1
